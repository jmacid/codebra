# Codebra - The classic snake game

This project was made in React.

To run the project: `npm run start`.

To build the project: `npm run build`.

To run the built project locally: `npm run serve:build`